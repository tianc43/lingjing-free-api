import{expect,test}from"playwright/test";test("PostgreSQL operator signs in and sees durable identity data",async({page})=>{await page.goto("http://127.0.0.1:18092/admin/");await page.getByLabel("Administrator password").fill("fixture-admin-password");await page.getByRole("button",{name:"Sign in"}).click();await expect(page.getByRole("heading",{name:"Accounts"})).toBeVisible();await page.getByRole("link",{name:"Users & projects"}).click();await expect(page.getByRole("heading",{name:"Users & projects"})).toBeVisible();await expect(page.getByRole("strong").filter({hasText:"Legacy user"})).toBeVisible();await expect(page.getByText("Legacy project")).toBeVisible();});test("PostgreSQL operator creates an identity and managed API key",async({page})=>{await page.goto("http://127.0.0.1:18092/admin/");await page.getByLabel("Administrator password").fill("fixture-admin-password");await page.getByRole("button",{name:"Sign in"}).click();await page.getByRole("link",{name:"Users & projects"}).click();await page.getByLabel("User name").fill("Browser User");await page.getByRole("button",{name:"Create user"}).click();await expect(page.getByText("Browser User").first()).toBeVisible();await page.getByLabel("Project name").fill("Browser Project");await page.getByLabel("Owner").selectOption({label:"Browser User"});await page.getByRole("button",{name:"Create project"}).click();await page.getByRole("link",{name:"API keys"}).click();await page.getByRole("button",{name:"Create API key"}).click();await page.getByLabel("Key name").fill("Browser Key");await page.getByLabel("Project").selectOption({label:"Browser User / Browser Project"});await page.getByRole("button",{name:"Create key"}).click();await expect(page.getByText("Copy it now")).toBeVisible();});test("PostgreSQL operator creates and assigns a video plan",async({page})=>{await page.goto("http://127.0.0.1:18092/admin/");await page.getByLabel("Administrator password").fill("fixture-admin-password");await page.getByRole("button",{name:"Sign in"}).click();await page.getByRole("link",{name:"Plans"}).click();await page.getByLabel("Plan name").fill("Browser Plan");await page.getByRole("button",{name:"Create plan"}).click();await expect(page.getByRole("strong").filter({hasText:"Browser Plan"})).toBeVisible();await page.getByLabel("Project").selectOption({label:"Legacy project"});await page.locator("select").filter({has:page.locator("option",{hasText:"Browser Plan"})}).selectOption({label:"Browser Plan"});await page.getByRole("button",{name:"Assign plan"}).click();await expect(page.getByRole("strong").filter({hasText:"Browser Plan"})).toBeVisible();});test("PostgreSQL operator configures and toggles a webhook",async({page})=>{await page.goto("http://127.0.0.1:18092/admin/");await page.getByLabel("Administrator password").fill("fixture-admin-password");await page.getByRole("button",{name:"Sign in"}).click();await page.getByRole("link",{name:"Webhooks"}).click();await page.getByLabel("Project").selectOption({label:"Legacy project"});await page.getByLabel("HTTPS endpoint").fill("https://hooks.example.test/video");await page.getByRole("button",{name:"Save and rotate secret"}).click();await expect(page.getByText("https://hooks.example.test/video")).toBeVisible();await page.getByRole("button",{name:"Disable"}).click();await expect(page.getByRole("button",{name:"Enable"})).toBeVisible();});test("PostgreSQL operator runs a text-to-video playground job",async({page})=>{await page.goto("http://127.0.0.1:18092/admin/");await page.getByLabel("Administrator password").fill("fixture-admin-password");await page.getByRole("button",{name:"Sign in"}).click();await page.getByRole("link",{name:"Playground"}).click();await page.getByLabel("Generation type").getByRole("button",{name:"Video"}).click();await page.getByLabel("Model").selectOption("m");await page.getByLabel("Prompt").fill("Slow clouds over a mountain ridge");await page.getByRole("button",{name:/Run/}).click();await expect(page.getByText("queued",{exact:true})).toBeVisible();await page.getByRole("link",{name:"Tasks"}).click();await expect(page.getByRole("heading",{name:"Tasks"})).toBeVisible();});test("PostgreSQL admin stays usable on mobile and logs out",async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto("http://127.0.0.1:18092/admin/");await page.getByLabel("Administrator password").fill("fixture-admin-password");await page.getByRole("button",{name:"Sign in"}).click();for(const link of["Subscriptions","Plans","Usage"]){await page.getByLabel("Navigate").selectOption({label:link});const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth);expect(overflow).toBe(false);}await page.getByRole("button",{name:"Sign out"}).click();await expect(page.getByRole("heading",{name:"Admin sign in"})).toBeVisible();});test("PostgreSQL operator runs image-to-video with a persisted frame",async({page})=>{await page.goto("http://127.0.0.1:18092/admin/");await page.getByLabel("Administrator password").fill("fixture-admin-password");await page.getByRole("button",{name:"Sign in"}).click();await page.getByRole("link",{name:"Playground"}).click();await page.getByLabel("Generation type").getByRole("button",{name:"Video"}).click();await page.getByLabel("Video mode").selectOption("image-to-video");await page.getByLabel("Model").selectOption("m");await page.getByLabel("First frame").setInputFiles({name:"frame.png",mimeType:"image/png",buffer:Buffer.from("frame")});await page.getByLabel("Prompt").fill("Animate this frame slowly");await page.getByRole("button",{name:/Run/}).click();await expect(page.getByText("queued",{exact:true})).toBeVisible();});
