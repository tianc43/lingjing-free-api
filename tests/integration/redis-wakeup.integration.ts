import{OptionalRedisCoordinator,RedisCoordinator}from"../../src/coordination/redis-coordinator.js";const raw=await RedisCoordinator.connect(process.env["REDIS_URL"]??"redis://:fixture-redis@127.0.0.1:16379"),redis=new OptionalRedisCoordinator(raw);let received="";const subscription=await redis.subscribeJobs(jobId=>{received=jobId;});await redis.publishJob("job");for(let i=0;i<50&&!received;i++)await new Promise(resolve=>setTimeout(resolve,10));if(received!=="job")throw new Error("Redis wakeup missing");await subscription.close();await redis.close();console.log("redis wakeup integration passed");
