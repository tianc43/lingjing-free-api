import{bootstrapPostgres}from"../../src/persistence/postgres-bootstrap.js";import{createPostgresRepositoryGraph}from"../../src/persistence/postgres-repository-graph.js";import{createPostgresServerRuntime}from"../../src/persistence/postgres-server-runtime.js";const pg=await bootstrapPostgres(process.env["DATABASE_URL"]??"postgres://lingjing:fixture-postgres@127.0.0.1:15432/lingjing"),graph=createPostgresRepositoryGraph(pg);await graph.accounts.update("legacy",{enabled:true});const key=await graph.identities.createApiKey({userId:"usr_legacy",projectId:"prj_legacy",name:"E2E",scopes:["video:create","video:read"],expiresAt:null});let submitted=false;const transport={read:<T>(path:string)=>{if(path.includes("asset/list"))return Promise.resolve({data:{records:submitted?[{id:"a",taskId:"fixture-task",creationCode:"c",scene:"t2v",modelCode:"m",createTime:Date.now()+100,status:0}]:[]}}as T);if(path.includes("describeUserTask"))return Promise.resolve({data:[{id:"fixture-task",status:0}]}as T);return Promise.resolve({}as T);},submitOnce:<T>()=>{submitted=true;return Promise.resolve({}as T);},uploadApi:<T>()=>Promise.resolve({}as T),putSigned:()=>Promise.resolve({statusCode:200,headers:{}})},runtime=await createPostgresServerRuntime({graph,objects:{put:()=>Promise.reject(new Error("unused")),get:()=>Promise.resolve(null),remove:()=>Promise.resolve()},runtimes:{listEnabled:()=>[{record:{id:"legacy"},transport}]as never},workerId:"e2e",fetchOutput:()=>Promise.reject(new Error("unused")),webhookTransport:{send:()=>Promise.resolve({status:204})},retentionMs:1000});const created=await runtime.app.inject({method:"POST",url:"/v1/videos",headers:{authorization:["Bearer",key.secret].join(" ")},payload:{model:"m",prompt:"hello"}}),id=created.json<{id:string}>().id;for(let i=0;i<3;i++)await runtime.workers.tick();if((await graph.jobs.findById(id))?.status!=="processing")throw new Error("server worker lifecycle failed");await runtime.close();console.log("postgres server worker integration passed");
