import{bootstrapPostgres}from"../../src/persistence/postgres-bootstrap.js";import{createPostgresRepositoryGraph}from"../../src/persistence/postgres-repository-graph.js";import{PostgresQueueWorker}from"../../src/jobs/postgres-queue-worker.js";import{PostgresSubmitWorker}from"../../src/jobs/postgres-submit-worker.js";let now=100;const runtime=await bootstrapPostgres(process.env["DATABASE_URL"]??"postgres://lingjing:fixture-postgres@127.0.0.1:15432/lingjing"),graph=createPostgresRepositoryGraph(runtime,()=>now);await graph.accounts.update("legacy",{enabled:true});await graph.admissions.reserve({jobId:"j",userId:"usr_legacy",projectId:"prj_legacy",accountId:"legacy",kind:"video",sourceType:"text-to-video",model:"m",apiId:"api",modelCode:"code",spaceId:7,requestFingerprint:"f",quotedPoints:1,dayWindowStart:0,monthWindowStart:0,snapshot:{prompt:"x"}});await new PostgresQueueWorker(graph,"q",()=>now).runOnce();now=123;await new PostgresSubmitWorker(graph,{submitOnce:()=>Promise.resolve({kind:"accepted",upstreamTaskId:null,upstreamFingerprint:"hash"})},"s",()=>now).run("j");const job=await graph.jobs.findById("j");if(job?.submittedAt!==123||job.upstreamFingerprint!=="hash"||job.apiId!=="api"||job.modelCode!=="code"||job.spaceId!==7)throw new Error("submission evidence missing");await graph.close();console.log("postgres submit evidence passed");
