import{Pool}from"pg";import{PostgresExecutionRepository}from"../../src/generation/postgres-execution-repository.js";const pool=new Pool({connectionString:process.env["DATABASE_URL"]??"postgres://lingjing:fixture-postgres@127.0.0.1:15432/lingjing"});await pool.query(`CREATE TABLE provider_submissions(id text primary key,job_id text unique,account_id text,request_fingerprint text,upstream_fingerprint text,catalog_revision text,baseline_json jsonb,outcome text,ambiguity_reason text,created_at bigint,updated_at bigint);CREATE TABLE provider_correlations(id text primary key,submission_id text unique,job_id text unique,provider text,account_id text,upstream_task_id text,upstream_asset_id text,creation_code text,evidence_type text,confidence text,correlated_at bigint);CREATE UNIQUE INDEX provider_task_unique ON provider_correlations(provider,account_id,upstream_task_id) WHERE upstream_task_id IS NOT NULL;CREATE TABLE job_worker_leases(job_id text primary key,worker_id text,lease_token text,fencing_token bigint,lease_expires_at bigint);`);const repo=new PostgresExecutionRepository(pool);for(const jobId of["j1","j2"]){await repo.captureBaseline({jobId,accountId:"a",requestFingerprint:jobId,upstreamFingerprint:jobId,catalogRevision:"r",baselineAssetIds:[],capturedAt:1});await repo.markOutcome(jobId,["baseline_captured"],"submitted",null,2);}await repo.correlate({jobId:"j1",upstreamTaskId:"task",upstreamAssetId:"asset1",creationCode:"c1",correlatedAt:3});let conflict=false;try{await repo.correlate({jobId:"j2",upstreamTaskId:"task",upstreamAssetId:"asset2",creationCode:"c2",correlatedAt:4});}catch{conflict=true;}const second=await pool.query<{outcome:string;ambiguity_reason:string}>("SELECT outcome,ambiguity_reason FROM provider_submissions WHERE job_id='j2'");const row=second.rows[0];if(!conflict||row===undefined||row.outcome!=="correlation_ambiguous"||row.ambiguity_reason!=="upstream_task_already_bound")throw new Error("correlation ambiguity not persisted");await pool.end();console.log("postgres correlation integration passed");
