import{Pool}from"pg";import{PostgresAdmissionRepository}from"../../src/accounts/postgres-admission-repository.js";const pool=new Pool({connectionString:process.env["DATABASE_URL"]??"postgres://lingjing:fixture-postgres@127.0.0.1:15432/lingjing",max:4});await pool.query(`CREATE TABLE users(id text primary key,status text);CREATE TABLE plans(id text primary key,enabled boolean,daily_limit_points numeric,monthly_limit_points numeric);CREATE TABLE projects(id text primary key,user_id text,plan_id text,status text);CREATE TABLE accounts(id text primary key,enabled boolean,daily_point_limit numeric,monthly_point_limit numeric);CREATE TABLE jobs(id text primary key,user_id text,project_id text,account_id text,kind text,source_type text,model text,request_fingerprint text,status text,quoted_points numeric,quote_known boolean,created_at bigint,updated_at bigint);CREATE TABLE budget_entries(job_id text primary key,account_id text,quoted_points numeric,state text,day_window_start bigint,month_window_start bigint,created_at bigint,updated_at bigint);CREATE TABLE usage_ledger(id text primary key,job_id text,user_id text,project_id text,account_id text,entry_type text,points numeric,reason text,created_at bigint);INSERT INTO users VALUES('u','active');INSERT INTO plans VALUES('pl',true,10,100);INSERT INTO projects VALUES('p','u','pl','active');INSERT INTO accounts VALUES('a',true,100,100);`);const repo=new PostgresAdmissionRepository(pool,()=>100),base={userId:"u",projectId:"p",accountId:"a",kind:"video"as const,sourceType:"text-to-video",model:"m",quotedPoints:7,dayWindowStart:0,monthWindowStart:0};const results=await Promise.all([repo.reserve({...base,jobId:"j1",requestFingerprint:"a".repeat(64)}),repo.reserve({...base,jobId:"j2",requestFingerprint:"b".repeat(64)})]);if(results.sort().join(",")!==["created","project_quota_exhausted"].sort().join(","))throw new Error(`oversell: ${results.join()}`);const count=await pool.query<{count:string}>("SELECT COUNT(*) count FROM jobs");const row=count.rows[0];if(!row||Number(row.count)!==1)throw new Error("job count mismatch");await pool.end();console.log("postgres admission integration passed");
