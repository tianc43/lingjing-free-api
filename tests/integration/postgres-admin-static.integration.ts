import{bootstrapPostgres}from"../../src/persistence/postgres-bootstrap.js";import{createPostgresRepositoryGraph}from"../../src/persistence/postgres-repository-graph.js";import{createPostgresApiRuntime}from"../../src/persistence/postgres-api-runtime.js";const pg=await bootstrapPostgres(process.env["DATABASE_URL"]??"postgres://lingjing:fixture-postgres@127.0.0.1:15432/lingjing"),graph=createPostgresRepositoryGraph(pg),app=await createPostgresApiRuntime(graph,undefined,undefined,"admin");for(const path of["/admin/","/admin/identities","/admin/playground","/admin/plans","/admin/usage","/admin/webhooks","/admin/tasks","/admin/api-access","/admin/settings"]){const response=await app.inject({method:"GET",url:path});if(response.statusCode!==200||!response.body.includes("<!doctype html>"))throw new Error(`admin static failed ${path}`);}await app.close();await graph.close();console.log("postgres admin static passed");
