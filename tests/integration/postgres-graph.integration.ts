import{createDatabaseRuntime}from"../../src/persistence/runtime-factory.js";const runtime=await createDatabaseRuntime({databaseDriver:"postgres",databaseUrl:process.env["DATABASE_URL"]??"postgres://lingjing:fixture-postgres@127.0.0.1:15432/lingjing"});if(runtime.driver!=="postgres"||runtime.postgres.schemaVersion!==2)throw new Error("graph bootstrap failed");const account=await runtime.repositories.accounts.create({name:"Graph",priority:1,dailyPointLimit:10,monthlyPointLimit:100,maxConcurrency:1});if((await runtime.repositories.accounts.find(account.id))?.name!=="Graph")throw new Error("graph repository unavailable");await runtime.repositories.close();await runtime.repositories.close();console.log("postgres graph integration passed");
