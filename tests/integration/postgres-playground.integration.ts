import{bootstrapPostgres}from"../../src/persistence/postgres-bootstrap.js";import{createPostgresRepositoryGraph}from"../../src/persistence/postgres-repository-graph.js";import{createPostgresApiRuntime}from"../../src/persistence/postgres-api-runtime.js";const pg=await bootstrapPostgres(process.env["DATABASE_URL"]??"postgres://lingjing:fixture-postgres@127.0.0.1:15432/lingjing"),graph=createPostgresRepositoryGraph(pg);await graph.accounts.update("legacy",{enabled:true});const models=[{id:"video",apiId:"api",sceneCode:"t2v",modelCode:"code",spaceId:1,uploadStrategy:"general"as const,modes:["text-to-video"as const,"image-to-video"as const]}],objects=new Map<string,Buffer>(),store={put:(key:string,stream:NodeJS.ReadableStream,options:{expectedSize:number})=>{objects.set(key,Buffer.from("abc"));return Promise.resolve({key,size:options.expectedSize,openRead:()=>stream,remove:()=>Promise.resolve()});},get:()=>Promise.resolve(null),remove:()=>Promise.resolve()},app=await createPostgresApiRuntime(graph,store,models,"admin"),login=await app.inject({method:"POST",url:"/admin/api/login",payload:{password:"admin"}}),setCookie=login.headers["set-cookie"],cookie=(Array.isArray(setCookie)?setCookie[0]:setCookie)?.split(";")[0],csrf=login.json<{csrf_token:string}>().csrf_token;if(!cookie)throw new Error("login failed");const headers={cookie,"x-csrf-token":csrf},listed=await app.inject({method:"GET",url:"/admin/api/playground/models?type=video&mode=text-to-video",headers});if(listed.json<{models:unknown[]}>().models.length!==1)throw new Error("playground models failed");const run=await app.inject({method:"POST",url:"/admin/api/playground/run",headers,payload:{kind:"video",model:"video",prompt:"hello",mode:"text-to-video",parameters:{}}});if(run.statusCode!==200||run.json<{job:{status:string}}>().job.status!=="queued")throw new Error("playground run failed");const i2v=await app.inject({method:"POST",url:"/admin/api/playground/run",headers,payload:{kind:"video",model:"video",prompt:"i2v",mode:"image-to-video",input_image:"data:image/png;base64,YWJj",parameters:{}}});if(i2v.statusCode!==200)throw new Error("playground i2v failed");await app.close();await graph.close();console.log("postgres playground passed");
