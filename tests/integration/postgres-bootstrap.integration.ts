import{bootstrapPostgres}from"../../src/persistence/postgres-bootstrap.js";const runtime=await bootstrapPostgres(process.env["DATABASE_URL"]??"postgres://lingjing:fixture-postgres@127.0.0.1:15432/lingjing");if(runtime.schemaVersion!==2)throw new Error("bootstrap schema mismatch");const result=await runtime.pool.query<{application_name:string}>("SELECT current_setting('application_name') application_name");if(result.rows[0]?.application_name!=="lingjing-video-gateway")throw new Error("application name mismatch");await runtime.close();console.log("postgres bootstrap integration passed");
