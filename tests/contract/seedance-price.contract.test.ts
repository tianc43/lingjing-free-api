import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";describe("observed Seedance price fixtures",()=>{const fixture=JSON.parse(readFileSync("tests/fixtures/seedance-price-schema.fixture.json","utf8"))as{models:Array<{display_name:string;mode:string;points:number}>};it("keeps documented T2V and I2V acceptance prices explicit",()=>{expect(fixture.models).toEqual(expect.arrayContaining([expect.objectContaining({display_name:"Seedance 2.0 mini",mode:"text-to-video",points:92}),expect.objectContaining({display_name:"Seedance-1.5-pro",mode:"image-to-video",points:32})]));});});
