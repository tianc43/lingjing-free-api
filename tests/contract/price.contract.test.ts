import{describe,expect,it,vi}from"vitest";import{LingjingPriceService}from"../../src/lingjing/price-service.js";describe("Lingjing price contract",()=>{it("uses the observed calculatePrice endpoint and exact I2V body",async()=>{const observed=vi.fn(),read=<T>(path:string,init?:unknown)=>{observed(path,init);return Promise.resolve({result:{points:32}}as T);},service=new LingjingPriceService({read});const body={enablePriceQuery:true as const,priceQueryService:"wan3",params:{shortVender:"ali",shortSenceCode:"i2v",model_name:"wan3",duration:"5",resolution:"1080P"}};expect((await service.calculate(body)).points).toBe(32);expect(observed).toHaveBeenCalledWith("/joycreator/AIModelApiConsole/calculatePrice",{method:"POST",body});});});
