import{describe,expect,it,vi}from"vitest";import{S3ObjectStore}from"../../src/media/s3-object-store.js";import type{S3Client}from"@aws-sdk/client-s3";vi.mock("@aws-sdk/s3-request-presigner",()=>({getSignedUrl:vi.fn(()=>Promise.resolve("https://s3.example/signed"))}));describe("presigned upload",()=>{it("creates a scoped S3 PUT URL with required content type",async()=>{const store=new S3ObjectStore({}as S3Client,"bucket","prefix");const signed=await store.presignPut("projects/p/input",{contentType:"image/png",size:3,expiresInSeconds:900});expect(signed).toMatchObject({url:"https://s3.example/signed",headers:{"content-type":"image/png"}});expect(signed.expiresAt).toBeGreaterThan(Date.now());});});
