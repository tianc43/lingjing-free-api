import{describe,expect,it}from"vitest";import{accountSessionPaths}from"../../src/session/create-provider.js";describe("Postgres Cookie import paths",()=>{it("uses configured data directory for generated accounts",()=>{expect(accountSessionPaths({dataDirectory:"D:/private"},"acct_0123456789abcdef01234567").storageStatePath.replaceAll("\\","/")).toBe("D:/private/accounts/acct_0123456789abcdef01234567/storage-state.json");});});
