import{describe,expect,it,vi}from"vitest";import{postgresAccountView}from"../../src/admin/postgres-mappers.js";describe("Postgres account observation",()=>{it("maps refreshed runtime balances to the admin view",()=>{expect(postgresAccountView({id:"a",name:"A",enabled:true,priority:1,dailyPointLimit:10,monthlyPointLimit:100,maxConcurrency:2,healthStatus:"ready",lastErrorCode:null,membership:"pro",pointsBalance:12,totalBalance:15,lastCheckedAt:123,updatedAt:123})).toMatchObject({health_status:"ready",has_session:true,membership:"pro",points_balance:12,total_balance:15,last_checked_at:123});});it("persists needs-login fields through the account repository port",async()=>{const recordObservation=vi.fn((...args:[string,unknown])=>{void args;return Promise.resolve({id:"a",name:"A",enabled:true,priority:1,dailyPointLimit:0,monthlyPointLimit:0,maxConcurrency:1,healthStatus:"needs_login",lastErrorCode:"lingjing_session_missing",membership:null,pointsBalance:null,totalBalance:null,lastCheckedAt:1,updatedAt:1});});const result=await recordObservation("a",{healthStatus:"needs_login",lastErrorCode:"lingjing_session_missing",membership:null,pointsBalance:null,totalBalance:null,maxConcurrency:null});expect(result.healthStatus).toBe("needs_login");expect(recordObservation).toHaveBeenCalledOnce();});});
