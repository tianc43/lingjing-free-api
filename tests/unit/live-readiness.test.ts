import{mkdtemp,mkdir,rm,writeFile}from"node:fs/promises";import{tmpdir}from"node:os";import{join}from"node:path";import{afterEach,describe,expect,it}from"vitest";import{inspectLiveReadiness}from"../../src/cli/live-readiness.js";const roots:string[]=[];afterEach(async()=>{await Promise.all(roots.splice(0).map(path=>rm(path,{recursive:true,force:true})));});describe("Live readiness",()=>{it("counts only complete legacy and generated session pairs",async()=>{const root=await mkdtemp(join(tmpdir(),"live-ready-"));roots.push(root);const auth=join(root,"auth"),a=join(root,"accounts","account-a"),b=join(root,"accounts","account-b");await Promise.all([mkdir(auth,{recursive:true}),mkdir(a,{recursive:true}),mkdir(b,{recursive:true})]);await Promise.all([writeFile(join(auth,"storage-state.json"),"{}"),writeFile(join(auth,"session-profile.json"),"{}"),writeFile(join(a,"storage-state.json"),"{}"),writeFile(join(a,"session-profile.json"),"{}"),writeFile(join(b,"storage-state.json"),"{}")]);expect(await inspectLiveReadiness({storageStatePath:join(auth,"storage-state.json"),sessionProfilePath:join(auth,"session-profile.json"),dataDirectory:root})).toEqual({ready:true,readyAccounts:2,legacyReady:true,generatedReady:1});});it("returns zero without exposing missing account directories",async()=>{const root=await mkdtemp(join(tmpdir(),"live-empty-"));roots.push(root);expect(await inspectLiveReadiness({storageStatePath:join(root,"auth","storage-state.json"),sessionProfilePath:join(root,"auth","session-profile.json"),dataDirectory:root})).toEqual({ready:false,readyAccounts:0,legacyReady:false,generatedReady:0});});});
