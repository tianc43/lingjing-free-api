import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";describe("PostgreSQL schema draft",()=>{const sql=readFileSync("docs/architecture/postgresql-schema-v16.sql","utf8");it("preserves critical uniqueness and transaction tables",()=>{for(const token of["BEGIN;","budget_entries","usage_ledger","provider_task_unique","job_worker_leases","webhook_outbox","UNIQUE(job_id,event_type)"])expect(sql).toContain(token);});it("uses project-scoped idempotency",()=>{expect(sql).toContain("jobs(api_key_id,idempotency_key_hash)");});});
