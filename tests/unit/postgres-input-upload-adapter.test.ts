import{Readable}from"node:stream";import{describe,expect,it,vi}from"vitest";import{LingjingPostgresInputUploadAdapter}from"../../src/lingjing/postgres-input-upload-adapter.js";describe("PostgreSQL Lingjing input upload",()=>{it("passes persisted model scene and space context to the verified upload service",async()=>{const upload=vi.fn(()=>Promise.resolve({value:"v",filePath:"p",frameUrl:null,vendor:null})),context=vi.fn(()=>Promise.resolve({sceneCode:"i2v",modelCode:"model",spaceId:7,uploadStrategy:"materials"as const})),adapter=new LingjingPostgresInputUploadAdapter({}as never,{context},()=>({upload}));const media={filename:"f.png",contentType:"image/png",size:3,openRead:()=>Readable.from([Buffer.from("abc")]),dispose:()=>Promise.resolve()};expect(await adapter.upload("job",media)).toMatchObject({value:"v"});expect(context).toHaveBeenCalledWith("job");expect(upload).toHaveBeenCalledWith(media,{sceneCode:"i2v",modelCode:"model",spaceId:7});});});
