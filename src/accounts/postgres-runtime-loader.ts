import{LingjingClient}from"../lingjing/client.js";import{AccountService}from"../lingjing/account.js";import{CatalogService}from"../models/catalog.js";import{CapacityManager}from"../jobs/capacity.js";import{DiscoveryLock}from"../jobs/discovery-lock.js";import{createSessionProvider}from"../session/create-provider.js";import type{AppConfig}from"../config.js";import type{PostgresAccount,PostgresAccountRepository}from"./postgres-account-repository.js";import type{AccountRuntime}from"./runtime.js";
export class PostgresRuntimeLoader{private readonly runtimes=new Map<string,AccountRuntime>();constructor(private readonly accounts:PostgresAccountRepository,private readonly config:AppConfig){}async refresh(accountId:string){const account=(await this.accounts.list()).find(item=>item.id===accountId);if(!account||!account.enabled){this.runtimes.delete(accountId);return null;}this.runtimes.delete(accountId);await this.loadOne(account);return this.runtimes.get(accountId)??null;}async load(){for(const account of await this.accounts.list())if(account.enabled)await this.loadOne(account);return this;}private async loadOne(account:PostgresAccount){try{const session=await createSessionProvider(this.config,account.id==="legacy"?undefined:account.id);await session.load();await session.loadProfile();const transport=new LingjingClient({session}),service=new AccountService({read:transport.read.bind(transport),session,config:this.config}),snapshot=await service.describe();const observed=await this.accounts.recordObservation(account.id,{healthStatus:"ready",lastErrorCode:null,membership:snapshot.membership,pointsBalance:snapshot.pointsBalance,totalBalance:snapshot.totalBalance,maxConcurrency:snapshot.maxConcurrency});const record={id:account.id,name:account.name,enabled:account.enabled,priority:account.priority,dailyPointLimit:account.dailyPointLimit,monthlyPointLimit:account.monthlyPointLimit,authDirectory:account.id==="legacy"?"data/auth":`data/accounts/${account.id}`,healthStatus:"ready"as const,lastErrorCode:null,subjectHash:snapshot.subject,membership:snapshot.membership,pointsBalance:snapshot.pointsBalance,totalBalance:snapshot.totalBalance,maxConcurrency:snapshot.maxConcurrency,lastCheckedAt:Date.now(),lastSelectedAt:null,createdAt:account.updatedAt,updatedAt:observed.updatedAt};this.runtimes.set(account.id,{record,session,transport,account:service,catalog:new CatalogService(transport,this.config.modelCacheTtlMs),capacity:new CapacityManager(snapshot.maxConcurrency,this.config.maxQueuedRequests),discoveryLock:new DiscoveryLock()});}catch{this.runtimes.delete(account.id);}}listEnabled(){return[...this.runtimes.values()];}close(){this.runtimes.clear();return Promise.resolve();}}
