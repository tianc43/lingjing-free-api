import{join}from"node:path";import{S3Client}from"@aws-sdk/client-s3";import type{LingjingTransport}from"../lingjing/types.js";import type{AppConfig}from"../config.js";import{bootstrapPostgres}from"./postgres-bootstrap.js";import{createPostgresRepositoryGraph}from"./postgres-repository-graph.js";import{createPostgresServerRuntime}from"./postgres-server-runtime.js";import{LocalObjectStore}from"../media/object-store.js";import{S3ObjectStore}from"../media/s3-object-store.js";import{PostgresRuntimeLoader}from"../accounts/postgres-runtime-loader.js";import{postgresVideoModels}from"../models/postgres-catalog-adapter.js";import{CatalogService}from"../models/catalog.js";import{SafeWebhookTransport}from"../webhooks/transport.js";import{OptionalRedisCoordinator,RedisCoordinator}from"../coordination/redis-coordinator.js";import{RemoteMediaFetcher}from"../media/remote-fetcher.js";import{createTempBudget}from"../media/temp-budget.js";export async function startPostgresEntrypoint(config:AppConfig,transport:LingjingTransport,runtimeLookup?:import("../lingjing/postgres-account-transport-resolver.js").RuntimeLookup){if(config.databaseUrl===null)throw new Error("DATABASE_URL is required for PostgreSQL");const pg=await bootstrapPostgres(config.databaseUrl),graph=createPostgresRepositoryGraph(pg),objects=config.objectStore.mode==="local"?new LocalObjectStore(join(config.dataDirectory,"objects")):new S3ObjectStore(new S3Client({region:config.objectStore.region,...(config.objectStore.endpoint===null?{}:{endpoint:config.objectStore.endpoint}),forcePathStyle:config.objectStore.forcePathStyle}),config.objectStore.bucket,config.objectStore.prefix),tempBudget=createTempBudget(config.maxTempBytes),fetcher=new RemoteMediaFetcher({tempDirectory:join(config.dataDirectory,"tmp"),tempBudget,requestBudget:tempBudget});const loadedRuntimes=runtimeLookup??await new PostgresRuntimeLoader(graph.accounts,config).load(),enabled=loadedRuntimes.listEnabled(),catalogModels=[];for(const accountRuntime of enabled){const snapshot=await accountRuntime.account.describe();catalogModels.push(...postgresVideoModels([...(await accountRuntime.catalog.list("text-to-video")),...(await accountRuntime.catalog.list("image-to-video"))],snapshot.spaceId));}const models=catalogModels.length>0?catalogModels:postgresVideoModels([...(await new CatalogService(transport,config.modelCacheTtlMs).list("text-to-video")),...(await new CatalogService(transport,config.modelCacheTtlMs).list("image-to-video"))],0);if(models.length===0)throw new Error("No Lingjing video models are available");const redis=new OptionalRedisCoordinator(config.redisUrl===null?null:await RedisCoordinator.connect(config.redisUrl).catch(()=>null));const runtime=await createPostgresServerRuntime({graph,objects,runtimes:enabled.length>0?loadedRuntimes:{listEnabled:()=>[{record:{id:"legacy"},transport}]as never},workerId:`postgres-${process.pid.toString()}`,models,fetchOutput:url=>fetcher.fetch(url,{kind:"video",maxBytes:config.maxVideoBytes}),webhookTransport:{send:async input=>({status:await new SafeWebhookTransport().post(input.url,input.headers,input.body)})},retentionMs:config.outputRetentionMs,adminPassword:config.adminPassword,...(loadedRuntimes instanceof PostgresRuntimeLoader?{runtimeRefresher:loadedRuntimes}:{}),redis});const close=runtime.close.bind(runtime);runtime.close=async()=>{await close();if(loadedRuntimes instanceof PostgresRuntimeLoader)await loadedRuntimes.close();await redis.close();};await runtime.app.listen({host:config.host,port:config.port});runtime.start();return runtime;}
