import Fastify,{type FastifyInstance}from"fastify";import type{PostgresRepositoryGraph}from"./postgres-repository-graph.js";import{postgresBearerAuth}from"../api/postgres-auth.js";import{requestPrincipal}from"../api/principal.js";import{registerErrorHandler}from"../api/error-handler.js";import{registerPostgresVideoRoutes}from"../api/routes/postgres-videos.js";import{registerPostgresAssetRoutes}from"../api/routes/postgres-assets.js";import type{ObjectStore}from"../media/object-store.js";import{registerPostgresUploadRoutes}from"../api/routes/postgres-uploads.js";import type{OptionalRedisCoordinator}from"../coordination/redis-coordinator.js";import{registerAdminStatic}from"../admin/static.js";import{registerPostgresAdminRoutes}from"../admin/postgres-routes.js";import{PostgresModelCatalog,type PostgresVideoModel}from"../models/postgres-model-catalog.js";export async function createPostgresApiRuntime(graph:PostgresRepositoryGraph,objects?:ObjectStore,models:readonly PostgresVideoModel[]=[{id:"m",apiId:"m",sceneCode:"t2v",modelCode:"",spaceId:0,uploadStrategy:"general",modes:["text-to-video","image-to-video"]}],adminPassword?:string,redis?:OptionalRedisCoordinator,runtimeRefresher?:{refresh(accountId:string):Promise<import("../accounts/runtime.js").AccountRuntime|null>},dataDirectory?:string,quote?:{quote(input:{modelId:string;mode:"text-to-video"|"image-to-video";duration?:number;resolution?:string;accountId:string}):Promise<number>}):Promise<FastifyInstance>{const app=Fastify({logger:false});registerErrorHandler(app);if(adminPassword!==undefined)registerPostgresAdminRoutes(app,graph,adminPassword,models,objects,runtimeRefresher,dataDirectory,quote);await registerAdminStatic(app,adminPassword!==undefined);app.get("/healthz",async()=>{await graph.runtime.pool.query("SELECT 1");return{status:"ok",database:"postgres",schema_version:graph.runtime.schemaVersion};});app.register(protectedApp=>{protectedApp.addHook("onRequest",postgresBearerAuth(graph.identities));registerPostgresVideoRoutes(protectedApp,graph,new PostgresModelCatalog(models),quote,redis);if(objects!==undefined){registerPostgresAssetRoutes(protectedApp,graph,objects);registerPostgresUploadRoutes(protectedApp,graph,objects);}protectedApp.get("/v1/principal",request=>{const principal=requestPrincipal(request);return{user_id:principal.userId,project_id:principal.projectId,api_key_id:principal.apiKeyId,scopes:[...principal.scopes].sort()};});});await app.ready();return app;}
