import type{FastifyInstance}from"fastify";import type{ObjectStore}from"../media/object-store.js";import type{RuntimeLookup}from"../lingjing/postgres-account-transport-resolver.js";import{createPostgresProtocolWorkers}from"../lingjing/postgres-protocol-worker-factory.js";import{createPostgresApiRuntime}from"./postgres-api-runtime.js";import type{PostgresRepositoryGraph}from"./postgres-repository-graph.js";import{PostgresWorkerOrchestrator}from"../jobs/postgres-worker-orchestrator.js";import{PostgresReconciliationWorker}from"../jobs/postgres-reconciliation-worker.js";import{PostgresArchiveWorker}from"../jobs/postgres-archive-worker.js";import{PostgresWebhookDeliveryWorker,type WebhookSendPort}from"../webhooks/postgres-delivery-worker.js";import type{PreparedMedia}from"../media/types.js";export async function createPostgresServerRuntime(input:{graph:PostgresRepositoryGraph;objects:ObjectStore;runtimes:RuntimeLookup;workerId:string;fetchOutput:(url:URL)=>Promise<PreparedMedia>;webhookTransport:WebhookSendPort;retentionMs:number;models?:readonly import("../models/postgres-model-catalog.js").PostgresVideoModel[];adminPassword?:string|null;runtimeRefresher?:{refresh(accountId:string):Promise<import("../accounts/runtime.js").AccountRuntime|null>};redis?:import("../coordination/redis-coordinator.js").OptionalRedisCoordinator}):Promise<{app:FastifyInstance;workers:PostgresWorkerOrchestrator;start():void;close():Promise<void>}>{const protocol=createPostgresProtocolWorkers(input),app=await createPostgresApiRuntime(input.graph,input.objects,input.models,input.adminPassword??undefined,input.redis,input.runtimeRefresher),reconcile=new PostgresReconciliationWorker(input.graph,protocol.poller,`${input.workerId}-reconcile`),archive=new PostgresArchiveWorker(input.graph,input.objects,input.fetchOutput,`${input.workerId}-archive`,input.retentionMs),webhooks=new PostgresWebhookDeliveryWorker(input.graph.webhooks,input.webhookTransport,`${input.workerId}-webhook`),workers=new PostgresWorkerOrchestrator(input.graph,{...protocol,reconcile,archive,webhooks});let closed=false,subscription:Awaited<ReturnType<NonNullable<typeof input.redis>["subscribeJobs"]>>|null=null;return{app,workers,start:()=>{workers.start();if(input.redis)void input.redis.subscribeJobs(()=>void workers.tick()).then(value=>{subscription=value;});},close:async()=>{if(closed)return;closed=true;await subscription?.close();await workers.close();await app.close();await input.graph.close();}};}
