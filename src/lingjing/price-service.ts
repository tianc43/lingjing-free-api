import type{LingjingTransport}from"./types.js";export interface PriceQuery{enablePriceQuery:true;priceQueryService:string;params:Record<string,string|number|boolean>;}
export interface PriceQuote{points:number;raw:unknown;}
export class LingjingPriceService{constructor(private readonly transport:Pick<LingjingTransport,"read">){}async calculate(query:PriceQuery):Promise<PriceQuote>{const raw=await this.transport.read<unknown>("/joycreator/AIModelApiConsole/calculatePrice",{method:"POST",body:query}),points=findPoints(raw);if(points===null)throw new Error("Lingjing price response has no point quote");return{points,raw};}}
function findPoints(value:unknown):number|null{if(typeof value==="number"&&Number.isFinite(value)&&value>=0)return value;if(typeof value!=="object"||value===null)return null;const row=value as Record<string,unknown>;for(const key of["points","point","costPoints","totalPoints","price"]){const candidate=row[key];if(typeof candidate==="number"&&Number.isFinite(candidate)&&candidate>=0)return candidate;if(typeof candidate==="string"&&candidate.trim()!==""&&Number.isFinite(Number(candidate))&&Number(candidate)>=0)return Number(candidate);}for(const child of Object.values(row)){const nested=findPoints(child);if(nested!==null)return nested;}return null;}
