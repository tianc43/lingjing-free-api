import{LingjingUploadService}from"../uploads/upload-service.js";import type{UploadService}from"../uploads/types.js";import type{PreparedMedia,UploadedMaterial}from"../media/types.js";import type{LingjingTransport}from"./types.js";export interface UploadContextResolver{context(jobId:string):Promise<{sceneCode:string;modelCode:string;spaceId:number;uploadStrategy?:"general"|"materials"}>;}
export class LingjingPostgresInputUploadAdapter{constructor(private readonly transport:LingjingTransport,private readonly contexts:UploadContextResolver,private readonly factory?:(strategy:"general"|"materials"|undefined)=>UploadService){}async upload(jobId:string,media:PreparedMedia):Promise<UploadedMaterial>{const context=await this.contexts.context(jobId),service=this.factory?.(context.uploadStrategy)??new LingjingUploadService(this.transport,{uploadStrategy:context.uploadStrategy??"general"});return service.upload(media,{sceneCode:context.sceneCode,modelCode:context.modelCode,spaceId:context.spaceId});}}
