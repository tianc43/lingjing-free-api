import {useState} from "react";import type{Project,WebhookDelivery,WebhookEndpoint}from"../types";
export function WebhooksPage({projects,webhooks,deliveries,onConfigure,onToggle,onReplay}:{projects:Project[];webhooks:WebhookEndpoint[];deliveries:WebhookDelivery[];onConfigure(projectId:string,url:string):Promise<WebhookEndpoint>;onToggle(endpoint:WebhookEndpoint):Promise<void>;onReplay(id:string):Promise<void>}){const[projectId,setProjectId]=useState("");const[url,setUrl]=useState("");const[secret,setSecret]=useState("");const[error,setError]=useState("");const[pending,setPending]=useState<string|null>(null);const submit=async(e:React.FormEvent)=>{e.preventDefault();setError("");try{const endpoint=await onConfigure(projectId,url);setSecret(endpoint.secret);setUrl("");}catch(c){setError(c instanceof Error?c.message:"无法配置 Webhook");}};return <><header className="page-heading"><div><p className="eyebrow">事件投递</p><h1 tabIndex={-1}>Webhook</h1><p>无需轮询或重新提交任务即可投递视频终态事件。</p></div></header>{secret&&<section className="command-notice"><span>请立即复制此签名密钥。</span><code>{secret}</code></section>}{error&&<p role="alert" className="field-error">{error}</p>}<div className="identity-layout"><section className="data-region"><h2>配置端点</h2><form className="inline-create" onSubmit={e=>void submit(e)}><label>项目<select required value={projectId} onChange={e=>setProjectId(e.target.value)}><option value="">选择项目</option>{projects.filter(p=>p.status==="active").map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>HTTPS 端点<input type="url" required placeholder="https://example.com/webhooks/lingjing" value={url} onChange={e=>setUrl(e.target.value)}/></label><button disabled={!projectId||!url}>保存并轮换密钥</button></form></section><section className="data-region"><h2>端点</h2><div className="identity-list">{webhooks.length===0?<p className="empty-state">尚未配置 Webhook 端点。</p>:webhooks.map(endpoint=><article className="identity-row" key={endpoint.id}><div><strong>{projects.find(p=>p.id===endpoint.project_id)?.name??endpoint.project_id}</strong><code>{endpoint.url}</code></div><span className={`status-pill status-${endpoint.enabled?"ready":"unknown"}`}>{endpoint.enabled?"Enabled":"Disabled"}</span><button className="quiet-button" disabled={pending===endpoint.id} onClick={()=>{setPending(endpoint.id);setError("");void onToggle(endpoint).catch((c:unknown)=>setError(c instanceof Error?c.message:"Could not update endpoint")).finally(()=>setPending(null));}}>{endpoint.enabled?"Disable":"Enable"}</button></article>)}</div></section></div><section className="data-region"><h2>投递历史</h2>{deliveries.length===0?<p className="empty-state">尚无排队事件。</p>:<table className="tasks-table"><thead><tr><th>事件</th><th>项目</th><th>状态</th><th>尝试次数</th><th>最近错误</th><th>操作</th></tr></thead><tbody>{deliveries.map(item=><tr key={item.id}><td data-label="Event">{item.event_type}</td><td data-label="Project">{projects.find(p=>p.id===item.project_id)?.name??item.project_id}</td><td data-label="Status">{item.status}</td><td data-label="Attempts">{item.attempts}</td><td data-label="Last error">{item.last_error??"—"}</td><td data-label="Action">{item.status==="dead"&&<button className="quiet-button" disabled={pending===item.id} onClick={()=>{setPending(item.id);setError("");void onReplay(item.id).catch((c:unknown)=>setError(c instanceof Error?c.message:"Could not replay event")).finally(()=>setPending(null));}}>重放</button>}</td></tr>)}</tbody></table>}</section></>}
