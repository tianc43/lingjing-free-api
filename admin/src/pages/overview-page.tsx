import type { Overview } from "../types";
import { StatusPill } from "../components/status-pill";

export function OverviewPage({ overview }: { overview: Overview }) {
  return <><header className="page-heading"><div><p className="eyebrow">Operational state</p><h1>Overview</h1></div><strong className="hero-metric">{overview.jobs.active}<small>active jobs</small></strong></header><section className="overview-strip" aria-label="Account summary"><div><span>Ready accounts</span><strong>{overview.accounts.ready}</strong></div><div><span>Unhealthy accounts</span><strong>{overview.accounts.unhealthy}</strong></div><div><span>Budget exhausted</span><strong>{overview.accounts.budget_exhausted}</strong></div><div><span>Today charged / reserved</span><strong>{overview.usage.daily_used_points} / {overview.usage.daily_reserved_points}</strong></div><div><span>Month charged / reserved</span><strong>{overview.usage.monthly_used_points} / {overview.usage.monthly_reserved_points}</strong></div></section><section className="data-region"><h2>Recent failures</h2>{overview.recent_failures.length === 0 ? <p className="empty-state">No recent failures. The work queue is clear.</p> : <table><thead><tr><th>Task</th><th>Account</th><th>Status</th><th>Error</th></tr></thead><tbody>{overview.recent_failures.map((job) => <tr key={job.id}><td className="mono">{job.id}</td><td>{job.account_name}</td><td><StatusPill status="unhealthy" /></td><td>{job.error_code ?? "—"}</td></tr>)}</tbody></table>}</section></>;
}
