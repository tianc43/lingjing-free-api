import { useState } from "react";
import type { Plan, Project } from "../types";
export function PlansPage({plans,projects,onCreate,onAssign}:{plans:Plan[];projects:Project[];onCreate(input:Omit<Plan,"id"|"created_at"|"updated_at">):Promise<void>;onAssign(projectId:string,planId:string):Promise<void>}){
 const [name,setName]=useState("");const [modes,setModes]=useState<Plan["allowed_modes"]>(["text-to-video"]);const [models,setModels]=useState("");const [duration,setDuration]=useState(5);const [resolutions,setResolutions]=useState("720p");const [daily,setDaily]=useState(0);const [monthly,setMonthly]=useState(0);const [concurrency,setConcurrency]=useState(0);const [queued,setQueued]=useState(0);const [projectId,setProjectId]=useState("");const [planId,setPlanId]=useState("");const [error,setError]=useState("");const[pending,setPending]=useState(false);
 const submit=async(e:React.FormEvent)=>{e.preventDefault();setError("");try{await onCreate({name:name.trim(),enabled:true,allowed_modes:modes,allowed_models:models.split(",").map(v=>v.trim()).filter(Boolean),max_duration_seconds:duration,allowed_resolutions:resolutions.split(",").map(v=>v.trim()).filter(Boolean),daily_limit_points:daily,monthly_limit_points:monthly,max_concurrency:concurrency,max_queued_requests:queued});setName("");}catch(c){setError(c instanceof Error?c.message:"无法创建套餐");}};
 return <><header className="page-heading"><div><p className="eyebrow">访问策略</p><h1 tabIndex={-1}>套餐</h1><p>在请求到达上游账号池前限制视频能力。</p></div></header>{error&&<p role="alert" className="field-error">{error}</p>}<div className="identity-layout"><section className="data-region"><h2>创建套餐</h2><form className="inline-create" onSubmit={e=>void submit(e)}><label>套餐名称<input required value={name} onChange={e=>setName(e.target.value)}/></label><fieldset><legend>允许的模式</legend>{(["text-to-video","image-to-video"] as const).map(mode=><label className="check-row" key={mode}><input type="checkbox" checked={modes.includes(mode)} onChange={e=>setModes(current=>e.target.checked?[...current,mode]:current.filter(v=>v!==mode))}/>{mode}</label>)}</fieldset><label>允许的模型 ID <span>（逗号分隔；留空表示不限）</span><input value={models} onChange={e=>setModels(e.target.value)}/></label><label>最长时长（秒） <span>（0 表示不限）</span><input type="number" min="0" value={duration} onChange={e=>setDuration(Number(e.target.value))}/></label><label>允许的分辨率 <span>（逗号分隔；留空表示不限）</span><input value={resolutions} onChange={e=>setResolutions(e.target.value)}/></label><div className="limit-grid"><label>每日点数上限<input type="number" min="0" value={daily} onChange={e=>setDaily(Number(e.target.value))}/></label><label>每月点数上限<input type="number" min="0" value={monthly} onChange={e=>setMonthly(Number(e.target.value))}/></label></div><div className="limit-grid"><label>最大活动任务数<input type="number" min="0" value={concurrency} onChange={e=>setConcurrency(Number(e.target.value))}/></label><label>最大排队任务数<input type="number" min="0" value={queued} onChange={e=>setQueued(Number(e.target.value))}/></label></div><button disabled={!name.trim()||modes.length===0}>创建套餐</button></form></section><section className="data-region"><h2>分配项目</h2><form className="inline-create" onSubmit={e=>{e.preventDefault();setPending(true);setError("");void onAssign(projectId,planId).catch((c:unknown)=>setError(c instanceof Error?c.message:"无法分配套餐")).finally(()=>setPending(false));}}><label>项目<select value={projectId} onChange={e=>setProjectId(e.target.value)}><option value="">选择项目</option>{projects.filter(p=>p.status==="active").map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>套餐<select value={planId} onChange={e=>setPlanId(e.target.value)}><option value="">选择套餐</option>{plans.filter(p=>p.enabled).map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><button disabled={!projectId||!planId||pending}>{pending?"分配ing…":"分配 plan"}</button></form><div className="identity-list">{plans.map(plan=><article className="identity-row" key={plan.id}><div><strong>{plan.name}</strong><span>{plan.allowed_modes.join(", ")} · {plan.max_concurrency?`${plan.max_concurrency} active`:"any concurrency"} · {plan.max_duration_seconds?`${plan.max_duration_seconds}s max`:"any duration"}</span><code>{plan.allowed_resolutions.join(", ")||"Any resolution"}</code></div><span className={`status-pill status-${plan.enabled?"ready":"unknown"}`}>{plan.enabled?"Active":"已禁用"}</span></article>)}</div></section></div></>;
}
